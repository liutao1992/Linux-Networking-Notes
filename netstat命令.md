`netstat` 是 Linux 系统中用于显示网络连接、路由表、接口统计信息、伪装连接、多播成员等网络相关信息的命令。尽管在较新版本的 Linux 中 `netstat` 已被 `ss` 和 `ip` 命令取代，但 `netstat` 仍然广泛用于日常排查和学习。

---

## 🧭 一、基本语法

```bash
netstat [选项]
```

常见用法：

```bash
netstat -tuln
```

---

## 📚 二、常用选项详解

| 选项   | 含义                       |
| ---- | ------------------------ |
| `-a` | 显示所有连接和监听端口              |
| `-t` | 显示 TCP 连接                |
| `-u` | 显示 UDP 连接                |
| `-l` | 仅显示监听状态的服务端口             |
| `-n` | 以 IP 地址和端口号显示（不进行反向域名解析） |
| `-p` | 显示哪个进程在使用该端口（需要 root 权限） |
| `-r` | 显示路由表信息（类似 `route -n`）   |
| `-i` | 显示网络接口信息（类似 `ifconfig`）  |
| `-s` | 显示网络统计信息（按协议分类）          |
| `-c` | 每隔一段时间刷新一次（持续输出）         |

---

## 🔍 三、典型用法示例

### 1. 查看所有正在监听的端口

```bash
netstat -tuln
```

显示 TCP 和 UDP 的监听端口，IP 和端口号为数字。

---

### 2. 查看所有网络连接和监听端口

```bash
netstat -an
```

结合 `-a` 和 `-n`，适合全面查看连接状态。

---

### 3. 查看某端口被哪个进程占用

```bash
sudo netstat -tunlp | grep 80
```

找出占用 TCP/UDP 80 端口的进程和程序。

---

### 4. 查看当前路由表

```bash
netstat -rn
```

与 `route -n` 类似，显示主机的路由转发信息。

---

### 5. 显示每种协议的统计信息

```bash
netstat -s
```

显示 TCP、UDP、ICMP 等协议的收发包统计，非常适合排查网络异常。

---

### 6. 持续监控网络连接状态

```bash
netstat -c
```

每秒刷新一次网络连接状态，实时监控。

---

## 🧠 四、输出内容解读（以 `netstat -tuln` 为例）

```text
Proto Recv-Q Send-Q Local Address           Foreign Address         State
tcp        0      0 0.0.0.0:22              0.0.0.0:*               LISTEN
tcp6       0      0 :::80                   :::*                    LISTEN
```

| 列名                | 含义                                            |
| ----------------- | --------------------------------------------- |
| `Proto`           | 协议类型（如 tcp、udp）                               |
| `Recv-Q`          | 接收队列长度（通常为 0，非0可能表示拥塞）                        |
| `Send-Q`          | 发送队列长度                                        |
| `Local Address`   | 本地地址和端口                                       |
| `Foreign Address` | 远端地址和端口（对监听端口为 `*`）                           |
| `State`           | 连接状态，如 `LISTEN`, `ESTABLISHED`, `TIME_WAIT` 等 |

---

## 📝 五、常见 TCP 状态解释

| 状态              | 含义             |
| --------------- | -------------- |
| `LISTEN`        | 监听连接           |
| `ESTABLISHED`   | 已建立连接          |
| `TIME_WAIT`     | 等待足够时间确保远程连接关闭 |
| `CLOSE_WAIT`    | 对端关闭连接，本地等待关闭  |
| `SYN_SENT`      | 客户端已发送连接请求     |
| `SYN_RECV`      | 服务端收到连接请求      |
| `FIN_WAIT1`/`2` | 主动关闭连接         |
| `LAST_ACK`      | 被动关闭连接中        |

---

## 🧪 六、推荐替代命令：`ss`

因为 `netstat` 属于 **net-tools** 工具集，该工具已不再维护。推荐使用更快更现代的 `ss` 命令：

```bash
ss -tuln   # 查看监听端口
ss -p      # 查看哪个进程使用端口
ss -s      # 查看统计信息
```

---

## ✅ 总结

* `netstat` 是一个强大的网络诊断工具，适合初学者了解网络连接、监听端口等基本概念；
* 常用于排查端口占用、网络阻塞、服务监听等问题；
* 虽然已被 `ss` 替代，但很多系统仍保留它作为兼容工具。



下面我们来**详细介绍 `netstat` 输出中的 `Recv-Q` 和 `Send-Q`**，它们在网络排查中非常关键。

---

## 🔍 `Recv-Q` 与 `Send-Q` 是什么？

它们分别代表了：

| 字段       | 含义                                             |
| -------- | ---------------------------------------------- |
| `Recv-Q` | 接收队列（Receive Queue）：对端发来但**本地还没被应用程序读取**的数据字节数 |
| `Send-Q` | 发送队列（Send Queue）：本地应用程序写入但**还没被系统发送出去**的数据字节数  |

你可以这样理解它们：

* 就像是一个“收发站”，`Recv-Q` 是你的“收件箱”，`Send-Q` 是你的“发件箱”。
* 如果数值不为 0，说明有**积压**数据。

---

## 🧠 示例说明

假设你执行：

```bash
netstat -nt
```

得到一行输出：

```text
Proto Recv-Q Send-Q Local Address      Foreign Address     State
tcp       512      0 192.168.1.10:80   192.168.1.50:34567  ESTABLISHED
```

### 表示含义：

* `Recv-Q = 512`：对方发来了 512 字节数据，你本地程序还没来得及读取；
* `Send-Q = 0`：你本地的数据已经都发出去了，对方还没阻塞你；

---

## 🧪 应用场景举例

### ✅ 正常情况：

```text
Recv-Q = 0，Send-Q = 0
```

说明收发双方通信顺畅，应用及时读取、发送数据。

---

### 🚨 异常情况 1：`Recv-Q` 大于 0

表示对方发来了数据，但是你本地程序**还没调用 `read()` 或 `recv()` 去读取**，可能出现：

* 程序读数据慢或阻塞；
* 程序逻辑有 bug；
* 接收缓冲区可能会被撑爆，最终导致连接被强制关闭。

---

### 🚨 异常情况 2：`Send-Q` 大于 0

表示你本地程序调用了 `write()` 或 `send()` 发数据，但系统**还没能成功地把数据送给对方**：

* 网络阻塞，带宽不足；
* 对方接收太慢（接收缓冲区满）；
* 系统发送缓冲区满了；
* 可能会出现 “写阻塞” 或 `Broken Pipe` 错误。

---

## 🛠 如何进一步诊断？

你可以搭配以下工具使用：

* `ss -nto`：显示发送/接收队列详细信息
* `netstat -anp | grep <port>`：定位具体端口的进程名
* `strace -p <pid>`：追踪进程是否在调用 `read()` / `write()`

---

## ✅ 总结

| 字段       | 表示的是什么 | 非 0 的可能原因           |
| -------- | ------ | ------------------- |
| `Recv-Q` | 接收队列   | 应用程序读取慢、阻塞、逻辑错误     |
| `Send-Q` | 发送队列   | 网络拥堵、对端接收慢、本地发送缓冲区满 |

这两个字段是诊断网络性能瓶颈、程序阻塞、连接异常的关键指标之一。

---


